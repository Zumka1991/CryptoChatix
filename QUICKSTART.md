# 🚀 Быстрый старт

## Запуск через Docker (Рекомендуется)

```bash
# Собрать и запустить одной командой
docker-compose up --build

# Открыть в браузере: http://localhost:5000
```

## Запуск для разработки

### 1. Запустить Backend

```bash
cd backend
dotnet run
```

Backend запустится на `http://localhost:5000`

### 2. Запустить Frontend (в новом терминале)

```bash
cd frontend
npm install
npm run dev
```

Frontend откроется на `http://localhost:3000`

## Тестирование E2E шифрования

1. Откройте два браузера (или обычное окно + инкогнито)
2. Зайдите на `http://localhost:3000` (или `http://localhost:5000` для Docker)
3. В первом окне введите имя: `Алиса`
4. Во втором окне введите имя: `Боб`
5. В списке онлайн появится второй пользователь
6. Выберите собеседника и начните чат
7. Все сообщения шифруются E2E! 🔒

## Установка пароля

1. Подключитесь к чату
2. Введите команду: `/pwd МойПароль123`
3. Пароль будет установлен
4. При следующем входе пароль потребуется!

**Примечание:** Пароли сохраняются в файл `backend/passwords.json` и сохраняются при перезапуске сервера.

## Проверка шифрования

Откройте DevTools (F12) → Network → WS → chatHub → Messages

Вы увидите, что все сообщения передаются в зашифрованном виде:
```json
{
  "key": "base64...",
  "iv": "base64...",
  "data": "base64..."
}
```

Сервер **не может прочитать** содержимое сообщений!
