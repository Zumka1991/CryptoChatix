# CryptoChatix 🔒

Минималистичный чат между двумя пользователями со сквозным шифрованием (E2E).

## Технологии

- **Backend**: ASP.NET 10.0 + SignalR (WebSocket)
- **Frontend**: React 18 + Vite
- **Шифрование**: Web Crypto API (RSA-OAEP + AES-GCM)

## Архитектура E2E шифрования

1. При подключении каждый пользователь генерирует пару RSA ключей (2048-bit)
2. Публичные ключи обмениваются через сервер
3. Для каждого сообщения:
   - Генерируется случайный AES-256 ключ
   - Сообщение шифруется AES-GCM
   - AES ключ шифруется публичным RSA ключом получателя
   - Сервер передает зашифрованное сообщение (не может его прочитать)
   - Получатель расшифровывает AES ключ своим приватным ключом
   - Получатель расшифровывает сообщение AES ключом

## Запуск для разработки

### Backend
```bash
cd backend
dotnet restore
dotnet run
# Сервер запустится на http://localhost:5000
```

### Frontend
```bash
cd frontend
npm install
npm run dev
# Приложение откроется на http://localhost:3000
```

## Запуск в Docker (Production)

```bash
# Собрать и запустить
docker-compose up --build

# Или отдельно:
docker build -t cryptochatix .
docker run -p 5000:5000 cryptochatix
```

Приложение будет доступно по адресу: http://localhost:5000

## Использование

1. Откройте приложение в двух разных браузерах/вкладках
2. Введите разные имена пользователей (пароль опционален)
3. Выберите собеседника из списка онлайн
4. Начните переписку - все сообщения шифруются E2E!

### Защита паролем

- По умолчанию пароль не требуется
- Используйте команду `/pwd НовыйПароль` в чате для установки пароля
- После установки пароля, он потребуется при следующем входе
- Пароли сохраняются в файл `backend/passwords.json` и сохраняются при перезапуске сервера

## Структура проекта

```
CryptoChatix/
├── backend/
│   ├── CryptoChatix.csproj
│   ├── Program.cs          # Настройка ASP.NET + SignalR
│   ├── ChatHub.cs          # SignalR Hub для обработки сообщений
│   ├── PasswordStorage.cs  # Хранение паролей в файле
│   ├── passwords.json      # Файл с паролями (создается автоматически)
│   └── appsettings.json
├── frontend/
│   ├── src/
│   │   ├── App.jsx         # Главный компонент чата
│   │   ├── crypto.js       # E2E шифрование (Web Crypto API)
│   │   ├── App.css         # Стили
│   │   └── main.jsx
│   ├── package.json
│   └── vite.config.js
├── Dockerfile              # Multi-stage build
└── docker-compose.yml
```

## Безопасность

- ✅ Сквозное шифрование (E2E) - сервер не может читать сообщения
- ✅ RSA-OAEP 2048-bit для обмена ключами
- ✅ AES-GCM 256-bit для шифрования сообщений
- ✅ Приватные ключи никогда не покидают браузер
- ⚠️  Это демо-проект - не используйте в production без дополнительной защиты:
  - Добавьте аутентификацию пользователей
  - Используйте HTTPS/WSS
  - Добавьте проверку подписи ключей
  - Реализуйте Perfect Forward Secrecy

## Требования

- .NET 10.0 SDK
- Node.js 20+
- Docker (для production сборки)
